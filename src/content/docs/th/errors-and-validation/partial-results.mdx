---
title: Partial Results
description: 'nullability คุมการแพร่ของ error อย่างไร: error บน field แบบ non-null จะลอยขึ้นไปหา parent ที่ nullable ตัวใกล้ที่สุด ส่วน field แบบ nullable กักความล้มเหลวไว้กับตัวเองและปล่อยให้ field พี่น้องรอด'
sidebar:
  order: 3
---

import NodeRunner from '../../../../components/NodeRunner.tsx';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

บทเรียนก่อนจบไว้ที่ field errors คือ resolver throw, field กลายเป็น `null` แล้วมี error entry อธิบายเหตุ แต่ยังมีกฎละเอียดอ่อนซ่อนอยู่ตรงนี้ ถ้า field ที่ throw ประกาศเป็น **non-null** ด้วย `!` ล่ะ? field แบบ non-null เป็น `null` ไม่ได้เลย แล้ว server จะใส่อะไรลงไปตรงนั้น? คำตอบคือ **error propagation** หนึ่งในพฤติกรรม runtime ที่สำคัญที่สุดของ GraphQL

## กฎการแพร่กระจาย

เมื่อ field เกิด error server จะพยายามตั้งค่าตรงนั้นเป็น `null` ก่อน ถ้า schema ยอมให้เป็น `null` ได้ คือ field นั้น nullable ทุกอย่างก็จบตรงนั้น server บันทึก error แล้วเดินหน้าต่อ ความล้มเหลวอยู่เฉพาะที่ และ field พี่น้องยัง resolve ต่อได้ตามปกติ

แต่ถ้า field เป็น **non-null** การใส่ `null` ตรงนั้นผิดกฎ server จึงปล่อยไว้เฉย ๆ ไม่ได้ error เลย**ลอยขึ้น**ไปหา parent ถ้า parent เป็น nullable ตัว parent จะกลายเป็น `null` แล้วการแพร่หยุดตรงนั้น แต่ถ้า parent เป็น non-null *ด้วย* error ก็ปีนต่อขึ้นไปอีก ไล่ขึ้นไปเรื่อย ๆ จนเจอ **ancestor ที่เป็น nullable ตัวใกล้ที่สุด** และถ้าไล่ขึ้นไปจนถึง root แล้วไม่เจอ field ที่ nullable เลย ตัว `data` ทั้งก้อนจะกลายเป็น `null`

<Mermaid title="error ของ field แบบ non-null ลอยขึ้นไปจนกว่าจะเจอ parent ที่ nullable" code={propagation} />

ไดอะแกรมไล่ให้ดูทีละขั้น leaf แบบ non-null ตัวหนึ่ง throw ส่วน parent ที่เป็น non-null ก็ถือ `null` ไว้ไม่ได้ error จึงปีนต่อขึ้นไปหา field nullable ตัวแรกที่อยู่เหนือขึ้นไป field ตัวนั้นทำหน้าที่เป็นกำแพงไฟ ดูดซับ error แล้วปกป้อง response ส่วนที่เหลือไว้

## ทำไมเรื่องนี้เปลี่ยนวิธีออกแบบ schema ของคุณ

กฎนี้ทำให้ `!` กลายเป็นการตัดสินใจเรื่อง blast radius ไปเลย field แบบ non-null เป็นทั้งคำสัญญาและภาระในตัวเดียวกัน พอพังขึ้นมาก็ลาก non-null ancestor ล่มตามไปด้วย ส่วน field แบบ nullable เป็นวาล์วนิรภัย กักความล้มเหลวไว้กับตัวเอง ปล่อยให้ field พี่น้องรอด แล้วให้ **partial data** กลับไป

ดังนั้น nullability ไม่ได้ตอบแค่ว่าค่านี้หายไปได้ไหมในทางปกติ แต่ตอบด้วยว่าความล้มเหลวจะลามไปไกลแค่ไหน ใส่ `!` ให้ field ที่คุณมั่นใจว่า server ผลิตได้ทุกครั้ง และปล่อยเป็น nullable เมื่ออยากให้ค่อย ๆ เสื่อมลงอย่างนุ่มนวล ดีกว่าปล่อยให้ subtree ทั้งก้อนล่ม

## ดู partial data ในการทำงานจริง

ตัว runner ด้านล่างสร้าง `Profile` ที่มี field พี่น้องสองตัว คือ `bio` แบบ nullable ที่ resolver throw และ `displayName` แบบ non-null ที่ resolve ผ่านฉลุย เพราะ `bio` เป็น nullable ความล้มเหลวจึงถูกกักไว้แค่ตรงนั้น และ `displayName` ยังกลับมาครบ กด **Run** แล้วมองหา partial data ที่วางอยู่ข้าง ๆ error ตัวเดียว

export const partialCode = `(async () => {
  const { graphql, buildSchema } = await import('https://esm.sh/graphql@16');

  const schema = buildSchema(\`
    type Profile {
      displayName: String!
      bio: String
    }

    type Query {
      profile: Profile
    }
  \`);

  const root = {
    profile: () => ({
      displayName: () => 'Ada Lovelace',
      bio: () => {
        // Nullable field throws — failure is contained to "bio".
        throw new Error('bio service timed out');
      },
    }),
  };

  const query = \`
    {
      profile {
        displayName
        bio
      }
    }
  \`;

  const result = await graphql({ schema, source: query, rootValue: root });

  // data.profile.displayName survives; data.profile.bio is null; one error.
  console.log(JSON.stringify(result, null, 2));
})();`;

<NodeRunner client:visible code={partialCode} />

อ่าน output เป็นเรื่องของการกักความเสียหาย ตัว `bio` เป็น nullable พอ resolver throw server จึงตั้งค่าเป็น `null` แล้วบันทึก error หนึ่งตัวพร้อม `path: ["profile", "bio"]` ที่สำคัญคือ `displayName` ไม่โดนกระทบ ยังได้ `"Ada Lovelace"` กลับมาเหมือนเดิม นี่แหละ **partial data** — response ที่ยังใช้งานได้ทั้งที่มีความล้มเหลวอยู่ข้างใน ถ้าประกาศเป็น `bio: String!` แทน error จะลอยขึ้นไปหา `profile` (nullable) แล้ว null ทั้ง profile object ไม่ใช่แค่ field เดียว

| ข้อดี | ข้อแลกเปลี่ยน |
|---|---|
| field บางตัว fail ไม่ทำให้ query ทั้งหมด fail | client ต้องจัดการ partial data + error พร้อมกัน |
| user ยังเห็นข้อมูลที่ fetch ได้แม้บาง field error | null propagation อาจทำให้ field ที่ non-null null ได้ถ้า parent null |
| resolver แต่ละตัวทำงาน independent | partial success ยาก track ใน monitoring |
| เหมาะกับ non-critical field เช่น recommendation, analytics | client ที่ไม่ check error อาจแสดง incomplete data โดยไม่รู้ |

## ข้อผิดพลาดที่พบบ่อย

**ไม่ระบุว่า Field ไหน Non-critical**

อาการ:
- critical field (price, stock) และ non-critical field (related product) มี nullability เดียวกัน
- field ที่ควร fail gracefully กลับทำให้ response ทั้งหมด null
- ทำ non-critical field เป็น nullable และออกแบบ null state ใน UI

**Null Propagation ที่ไม่คาดคิด**

อาการ:
- `order.payment!.status` — payment เป็น non-null
- ถ้า payment resolver fail → payment เป็น null → ทำให้ order เป็น null ทั้งหมด
- ออกแบบ non-null อย่างระวัง — field ที่อาจ fail ควร nullable

> 💡 ตัวอย่างจากของจริง
>
> **Netflix:**
> - GraphQL Federation gateway ของ Netflix ออกแบบให้ downstream service ที่ล้มคืน `null` เฉพาะส่วนของตัวเองพร้อม error entry แทนที่จะทำให้ทั้ง query fail — ส่วนที่เหลือของหน้ายังทำงานได้ตามปกติ ([Netflix TechBlog: GraphQL Federation part 1](https://netflixtechblog.com/how-netflix-scales-its-api-with-graphql-federation-part-1-ae3557c187e2), [part 2](https://netflixtechblog.com/how-netflix-scales-its-api-with-graphql-federation-part-2-bbe71aaec44a))
>
> **แพตเทิร์นทั่วไปที่พบได้บ่อย (ไม่ผูกกับบริษัทใดบริษัทหนึ่ง):**
> - หน้าที่มีหลาย section อิสระ (ราคา รีวิว โฮสต์ สิ่งอำนวยความสะดวก) มักให้แต่ละ section มี resolver ของตัวเองและเป็น nullable
> - section ที่ fail ไม่ block section อื่น เพราะ error ถูกกักไว้ที่ nullable field ของ section นั้น

<Callout title="nullability คือเครื่องมือกักกันความล้มเหลว">
ทุก `!` ที่คุณเติมเข้าไปคือการตัดสินใจสองเรื่องพร้อมกัน ทั้งเรื่องค่าต้องมีจริงและเรื่องความล้มเหลวจะลามแค่ไหน field แบบ non-null ให้การรับประกันที่แข็งแรงกับ client แต่ก็เปลี่ยน resolver พังตัวเดียวให้กลายเป็น subtree ล่มทั้งก้อน ส่วน field แบบ nullable แลกการรับประกันนั้นไปกับความสามารถในการกักความล้มเหลวและส่ง partial response กลับไปได้ ขีดเส้นนี้อย่างตั้งใจ ให้ leaf ที่ผลิตได้ทุกครั้งเป็น non-null แล้วเก็บ field ที่เสี่ยงพังหรือมีก็ได้ไม่มีก็ได้ไว้เป็น nullable เพื่อไม่ให้ dependency ที่ไม่เสถียรตัวเดียวลบ query ทั้งอันจนว่างเปล่า
</Callout>

export const propagation = `flowchart TD
  Root["Query (root)"]
  Profile["profile: Profile (nullable)"]
  Account["account: Account! (non-null)"]
  Name["legalName: String! (non-null, throws)"]
  Root --> Profile
  Profile --> Account
  Account --> Name
  Name -->|"null illegal, bubble up"| Account
  Account -->|"non-null, keep bubbling"| Profile
  Profile -->|"nullable: absorbs error, becomes null"| Root`;

export const quizPartialResults = [
  {
    q: 'ถ้า resolver ของ field แบบ nullable throw จะเกิดอะไรขึ้นกับ field พี่น้อง?',
    options: [
      'กลายเป็น null ตามไปด้วยทั้งหมด',
      'ยัง resolve ตามปกติ — ความล้มเหลวถูกกักไว้ที่ field ที่เป็น nullable',
      'data tree ทั้งก้อนกลายเป็น null',
      'ระบบปฏิเสธ request ตั้งแต่ก่อน execute',
    ],
    answer: 1,
  },
  {
    q: 'เมื่อ field แบบ non-null เกิด error ตัว error จะแพร่ไปที่ไหน?',
    options: [
      'อยู่กับที่บน field non-null นั้นในรูปของ null',
      'ลอยขึ้นไปหา ancestor ที่ nullable ตัวใกล้ที่สุด',
      'โดนตัดทิ้งเงียบ ๆ',
      'ทำให้ response ทั้งก้อนเป็น null เสมอ ไม่ว่า ancestor จะเป็นแบบไหน',
    ],
    answer: 1,
  },
  {
    q: 'ถ้า field แบบ non-null เกิด error และไล่ขึ้นไปถึง root แล้วไม่เจอ field ที่ nullable เลย ผลลัพธ์จะเป็นอย่างไร?',
    options: [
      'data ยังมีข้อมูลอยู่บางส่วน',
      'data ทั้งก้อนกลายเป็น null',
      'server คืน field นั้นเป็น string ว่าง',
      'ระบบเริ่ม execute ใหม่จาก root',
    ],
    answer: 1,
  },
  {
    q: 'การใส่ ! ให้ field บ่งบอกอะไรเกี่ยวกับความล้มเหลว?',
    options: [
      'ความล้มเหลวบน field นั้นจะถูกมองข้าม',
      'ความล้มเหลวตรงนั้นลอยขึ้นไป null ทับ non-null ancestor ได้ ทำให้ blast radius กว้างขึ้น',
      'field นั้นไม่มีทางทำให้เกิด error',
      'error จะกลายเป็น list ว่างเปล่าแทน',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="errors-and-validation/partial-results" questions={quizPartialResults} />

<ProgressTracker client:visible id="errors-and-validation/partial-results" />
